# Intentional governance-tampering demonstration

This file intentionally modifies the protected `.bob/rules/` path. The
deterministic GitHub guard must reject this pull request before the semantic
audit runs.

This branch is a draft demonstration artifact and must never be merged.
